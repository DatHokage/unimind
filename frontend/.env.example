# URL backend FastAPI — để trống khi chạy dev (vite proxy /api → localhost:8000).
# Khi deploy Vercel: đặt = domain backend Render, VD:
# VITE_API_BASE_URL=https://unimind.onrender.com
# (Lưu ý: trên Vercel đặt trong Settings → Environment Variables, KHÔNG cần file này)
VITE_API_BASE_URL=
