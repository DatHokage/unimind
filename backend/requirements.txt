fastapi>=0.116,<1.0
uvicorn[standard]
sqlalchemy>=2.0,<2.1
psycopg2-binary
alembic
python-jose[cryptography]
passlib==1.7.4
bcrypt==4.0.1
pydantic>=2
pydantic-settings
httpx
python-multipart
python-dotenv
pytest

# Chatbot quy chế (RAG) — embedding gọi qua Voyage AI API, KHÔNG tải model local:
# - chromadb: lưu + truy vấn vector store. Quan trọng: code luôn truyền vector
#   tường minh (query_embeddings / embeddings) — KHÔNG để Chroma tự nhúng văn
#   bản, nên onnxruntime (dependency của chromadb) không bao giờ được tải vào RAM.
# - python-docx: parse Sổ tay sinh viên (DOCX) khi rebuild vector store
#   (scripts/rebuild_vector_store.py — chạy local, gọi Voyage API để nhúng).
# LLM gọi qua HTTP thuần (httpx): OpenRouter + Gemini — KHÔNG cần SDK google.
chromadb>=1.0
python-docx>=1.1
